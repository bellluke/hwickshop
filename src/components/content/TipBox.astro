---
interface Props {
  author?: string;
}

const { author } = Astro.props;
---

<div class="my-6 border-l border-stone-200 pl-5 py-1">
  <div class="text-sm leading-relaxed text-[var(--color-text)]">
    <slot />
  </div>
  {author && (
    <p class="mt-2 text-xs text-[var(--color-text-muted)]">— {author}</p>
  )}
</div>
